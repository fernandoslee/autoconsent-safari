{
    "name": "Ensighten",
    "prehideSelectors": ["#ensNotifyBanner", ".ensModal"],
    "detectCmp": [
        {
            "any": [{ "visible": "#ensNotifyBanner[style*=block]" }, { "visible": "#ensModalWrapper[style*=block]" }]
        }
    ],
    "detectPopup": [
        {
            "any": [{ "visible": "#ensNotifyBanner[style*=block]" }, { "visible": "#ensModalWrapper[style*=block]" }]
        }
    ],
    "optIn": [{ "waitForThenClick": "#ensCloseBanner,#modalAcceptButton,#ensAcceptAll,#bannerAcceptButton" }],
    "optOut": [
        { "wait": 500 },
        {
            "if": { "exists": "#ensRejectAll,#rejectAll,#ensRejectBanner,.rejectAll,#bannerRejectButton,#ensCloseBanner" },
            "then": [{ "waitForThenClick": "#ensRejectAll,#rejectAll,#ensRejectBanner,.rejectAll,#bannerRejectButton,#ensCloseBanner" }],
            "else": [
                { "waitForThenClick": "#ensOpenModal,#modalOpenButton,#bannerOpenModal", "optional": true },
                { "waitFor": "#ensModalWrapper[style*=block]", "timeout": 1000 },
                { "click": ".ensCheckbox:checked", "all": true },
                { "waitForThenClick": "#ensSave,#modalSaveButton,#bannerSave" }
            ]
        }
    ]
}
